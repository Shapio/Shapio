# Copiez ce fichier en .env.local pour développer contre la BDD LOCALE (Docker).
# Récupérez les valeurs exactes via : npx supabase status
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=<copier "anon key" affichée par `npx supabase status`>
